pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh '''
                    node --version
                    netlify --version
                    npm --version
                    # node-jq --version
                    jq --version
                    serve --version
                '''
            }
        }
    }

    /*
    agent any

    stages {
        stage ('Build docker image') {
            steps {
                sh 'docker build -t my-playwright .'
            }
        }
    }
    */
}